#!/usr/bin/python3


def is_kind_of_class(obj, a_class):
    """
       A function which checks if an object is an instance of, or inherits
       from an instance of a given class.
    """
    return isinstance(obj, a_class)
